# trakt.tv
trakt_client_id="CLIENT_ID"
trakt_client_secret="CLIENT_SECRET"

# fanarttv
fanarttv_api_key="FANARTTV_KEY"

# attrakttv database location (will be created if needed)
# This is the path INSIDE the container and must match the target in docker-compose.yml if used
# Not that variables like $HOME will not be expanded
trakt_db_path=/home/shiny

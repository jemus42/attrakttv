% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{attrakttv_config}
\alias{attrakttv_config}
\title{Load runtime config}
\usage{
attrakttv_config(path = NULL)
}
\arguments{
\item{path}{Optional explicit path to a YAML config file.}
}
\value{
A named list, e.g. \code{list(refresh = list(tick_interval_hours = 12, ...))}.
}
\description{
Resolution order:
\enumerate{
\item \code{path} argument (if given)
\item \code{ATTRAKTTV_CONFIG} env var
\item \verb{/etc/attrakttv/config.yml} (container convention)
\item Baked-in \code{inst/config.default.yml}
}
}
\details{
User config (any of 1-3) is merged on top of the baked default with
\code{\link[utils:modifyList]{utils::modifyList()}}, which recurses into nested lists, so partial
overrides only need to set the keys they want to change.
}
\examples{
\dontrun{
cfg <- attrakttv_config()
cfg$refresh$tick_interval_hours
}
}
